import { Cabin, Lato } from 'next/font/google'

export const cabin = Cabin({
  weight: ['400', '700'],
  subsets: ['latin'],
})

export const lato = Lato({
  weight: ['100', '400', '700'],
  subsets: ['latin'],
})
